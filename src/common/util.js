


/*
const manager = {

  bubbleSortByKey: function (array, key) {
    var i = 0,
      len = array.length,
      j, d;
    for (; i < len; i++) {
      for (j = 0; j < len; j++) {
        if (array[i][key] < array[j][key]) {
          d = array[j];
          array[j] = array[i];
          array[i] = d;
        }
      }
    }
    return array;
  }

}


export default manager;
*/






